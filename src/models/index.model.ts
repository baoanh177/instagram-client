export enum Status {
  IDLE = "idle", // ? rảnh
  PENDING = "pending",
  FULFILLED = "fulfilled",
  REJECTED = "rejected",
}
export enum AuthActions {
  UNSET = "unset",
  LOGIN = "login",
  LOGOUT = "logout",
  REGISTER = "register",
}
export enum Gender {
  MALE = 0,
  FEMALE = 1,
  OTHER = 2,
}
